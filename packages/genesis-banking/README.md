# genesis-banking

Independent banking domain package for Genesis.

Depends only on `genesis-core` and demonstrates domain-specific YAML rules, parsing, loading, and conflict resolution without modifying Core.

The bundled rules are illustrative software examples, not financial advice, credit policy, or regulatory requirements.
