# genesis-core

Universal, domain-neutral rule engine for the Genesis platform.

## Install

```bash
pip install genesis-core
```

The package provides the generic rule model, evaluation engine, extension contracts, and installed-domain discovery. It does not depend on Medical, Banking, Construction, or any other domain package.

## Public API

See `genesis_core.api` and `docs/api-stability.md`.
