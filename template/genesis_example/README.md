# Genesis Example Domain

Replace `genesis_example` and implement the domain-specific parser, source, resolver, adapters, and knowledge.

The package must depend only on `genesis-core` and must register a `genesis.domains` entry point.
